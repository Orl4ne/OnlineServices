using OS.Common.DataAccessHelpers;
using OS.Common.AssessementServices.TransfertObjects;
using System.Collections.Generic;

namespace OS.Common.AssessementServices.Interfaces.Repositories
{
    public interface IFormRepository : IRepository<FormTO, int>
    {
        //Form Add(Form Entity);
        //IEnumerable<Form> GetAll();
        //Form GetByID(int Id);
        //bool Remove(Form entity);
        //bool Remove(int Id);
        //Form Update(Form Entity);
    }
}